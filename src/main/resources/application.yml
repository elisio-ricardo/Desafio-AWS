spring:
  application:
    name: desafioItau

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#  jdbc:mysql://localhost:3306/db_desafio_itau


  datasource:
    url: jdbc:mysql://localhost:3306/db_desafio_itau?createDatabaseIfNotExist=True&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: springuser
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver

  cloud:
    aws:
      credentials:
        access-key: ${ACCESS_KEY_AWS:AKIAUPMYMYADEKKFTGU2}
        secret-key: ${SECRET_KEY_AWS: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}
      region:
        static: ${REGION_AWS:us-east-2}
      sns:
        topic:
          book-topic:
            arn: ${AWS_ARN:arn:aws:sns:us-east-2:307946635270:book-topic}

server:
  port: 8080

# JPA RDS Configuration
# Uncomment and configure these lines if using AWS RDS
# spring:
#   datasource:
#     url: # put your AWS RDS URL here
#     driverClassName: com.mysql.cj.jdbc.Driver
#     username: ${dbUser}
#     password: ${dbPass}